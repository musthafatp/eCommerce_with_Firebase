name: ecommerce_with_firebase
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.0 <4.0.0'  # 👈 Slightly broader range for compatibility

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.15.2
  cloud_firestore: ^5.6.12
  firebase_auth: ^5.1.0
  firebase_storage: ^12.1.0
  cupertino_icons: ^1.0.2
  google_fonts: ^5.0.0
  flutter_screenutil: ^5.9.3
  intl_phone_field: ^3.2.0
  permission_handler: ^12.0.1
  location: ^8.0.1
  carousel_slider: ^5.1.1
  iconsax_flutter: ^1.0.1
  fluttertoast: ^9.0.0
  google_sign_in: ^7.2.0  
  flutter_facebook_auth: ^7.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  
  assets:
    - assets/images/
    
